/**
 * Enum that defines the supported field types for surveys.
 */
export enum SurveyFieldTypesEnum {
  TEXT = 'TEXT',
  EMAIL = 'EMAIL',
  PHONE_NUMBER = 'PHONE_NUMBER',
  DATE = 'DATE',
  TEXT_AREA = 'TEXT_AREA',
  SELECT = 'SELECT',
  RADIO = 'RADIO',
  CHECKBOX = 'CHECKBOX',
}
